import React from 'react';
import ReactDOM from 'react-dom';
import App from "./App";


//2 parameters for render (what do i want to render, where do i want to render)
ReactDOM.render(
    <App />,
     document.getElementById('root')
    );
